{% extends 'base.html.twig' %}

{% block body %}
    <h1>Creation planning semaine N+1</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" class="btn btn-success"  value="Create" />
    {{ form_end(form) }}

    <ul>
        <input type="button" class="btn btn-secondary" onclick="window.location.href='{{ path('home2_index') }}'" value="Back to the list" style="margin: 5px -40px;"/>
    </ul>
{% endblock %}
